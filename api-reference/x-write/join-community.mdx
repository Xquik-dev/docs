---
title: "How to Join a Community on Twitter via API"
sidebarTitle: "Join community"
description: "Learn how to join a Twitter Community by ID with one connected account. Track API responses, verify membership, and resolve invitation or eligibility failures."
api: "POST /x/communities/{id}/join"
keywords: ["how to join community on twitter", "how to join a community on twitter", "how to join a twitter community", "join twitter community", "X Community API", "Twitter Community membership"]
---

import LlmsDirective from "/snippets/llms-directive.mdx";
import WriteActionLifecycleResponse from "/snippets/write-action-lifecycle-response.mdx";

{/* GENERATED RESPONSE EXAMPLES START */}
<ResponseExample>
  ```json 200
  {
    "object": "x_write_action",
    "id": "12345",
    "writeActionId": "12345",
    "action": "like",
    "status": "success",
    "terminal": true,
    "retryable": false,
    "safeToRetry": false,
    "statusUrl": "/api/v1/x/write-actions/12345",
    "pollAfterMs": null
  }
  ```

  ```json 202 · x-write/join-community
  {
    "object": "x_write_action",
    "id": "12346",
    "writeActionId": "12346",
    "action": "create_tweet",
    "status": "dispatching",
    "terminal": false,
    "retryable": false,
    "safeToRetry": false,
    "statusUrl": "/api/v1/x/write-actions/12346",
    "pollAfterMs": 2000
  }
  ```

  ```json 400 · x-write/join-community
  {
    "error": "invalid_input",
    "message": "Invalid input. Check request fields.",
    "charged": false,
    "chargedCredits": "0",
    "retryable": false,
    "safeToRetry": true
  }
  ```

  ```json 401 · x-write/join-community
  {
    "error": "unauthenticated",
    "message": "Authentication required. Provide a valid API key or bearer token."
  }
  ```

  ```json 402 · x-write/join-community
  {
    "error": "insufficient_credits",
    "message": "Insufficient credits. Top up or subscribe to continue."
  }
  ```

  ```json 403 · x-write/join-community
  {
    "error": "account_needs_reauth",
    "message": "X account needs re-authentication. Re-add the account."
  }
  ```

  ```json 404 · x-write/join-community
  {
    "error": "account_not_found",
    "message": "X account not found. Connect it first at /dashboard/account?tab=x-accounts."
  }
  ```

  ```json 409 · x-write/join-community
  {
    "error": "idempotency_conflict",
    "message": "Idempotency-Key was already used with a different request.",
    "charged": false,
    "chargedCredits": "0",
    "retryable": false,
    "safeToRetry": true
  }
  ```

  ```json 422 · x-write/join-community
  {
    "error": "x_rejected",
    "message": "X rejected this request. Wait a few minutes and try again."
  }
  ```

  ```json 429 · x-write/join-community
  {
    "error": "rate_limit_exceeded",
    "message": "Too many requests. Try again later.",
    "retryAfter": 60
  }
  ```

  ```json 500 · x-write/join-community
  {
    "error": "x_write_failed",
    "message": "Write action failed unexpectedly. Contact support if this persists."
  }
  ```

  ```json 503 · x-write/join-community
  {
    "error": "write_tracking_unavailable",
    "message": "Write tracking unavailable. Try again.",
    "charged": false,
    "chargedCredits": "0",
    "retryable": true,
    "safeToRetry": true
  }
  ```
</ResponseExample>
{/* GENERATED RESPONSE EXAMPLES END */}
<LlmsDirective />

<Callout icon="coins" color="#5c3327">
  **10 credits per call** · [All plans](https://xquik.com/#pricing) from $0.00012/credit
</Callout>

<CodeGroup>
  ```bash cURL
  curl -X POST https://xquik.com/api/v1/x/communities/1893726451023847424/join \
    -H "x-api-key: xq_YOUR_KEY_HERE" \
    -H "Idempotency-Key: community-join-1895432178065391234" \
    -H "Content-Type: application/json" \
    -d '{
      "account": "myxhandle"
    }' | jq
  ```

  ```javascript Node.js
  const response = await fetch(
    "https://xquik.com/api/v1/x/communities/1893726451023847424/join",
    {
      method: "POST",
      headers: {
        "x-api-key": "xq_YOUR_KEY_HERE",
        "Idempotency-Key": "community-join-1895432178065391234",
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        account: "myxhandle",
      }),
    }
  );
  const data = await response.json();
  ```

  ```python Python
  import requests

  response = requests.post(
      "https://xquik.com/api/v1/x/communities/1893726451023847424/join",
      headers={
          "x-api-key": "xq_YOUR_KEY_HERE",
          "Idempotency-Key": "community-join-1895432178065391234",
      },
      json={
          "account": "myxhandle",
      },
  )
  data = response.json()
  ```

  ```go Go
  package main

  import (
      "bytes"
      "encoding/json"
      "fmt"
      "net/http"
  )

  func main() {
      body, _ := json.Marshal(map[string]interface{}{
          "account": "myxhandle",
      })

      req, err := http.NewRequest("POST", "https://xquik.com/api/v1/x/communities/1893726451023847424/join", bytes.NewReader(body))
      if err != nil {
          panic(err)
      }
      req.Header.Set("x-api-key", "xq_YOUR_KEY_HERE")
      req.Header.Set("Idempotency-Key", "community-join-1895432178065391234")
      req.Header.Set("Content-Type", "application/json")

      resp, err := http.DefaultClient.Do(req)
      if err != nil {
          panic(err)
      }
      defer resp.Body.Close()

      var data map[string]interface{}
      if err := json.NewDecoder(resp.Body).Decode(&data); err != nil {
          panic(err)
      }
      fmt.Println(data)
  }
  ```
</CodeGroup>

## How to Join Community on Twitter Through the API

Call `POST /x/communities/{id}/join` for one existing Community. Send one
connected Twitter account in the request body. Xquik returns a tracked write
action for that membership attempt.

This route does not discover Communities. It cannot create a Community or send
an invitation. It also cannot change membership rules. X controls whether the
selected account can join the specific Community.

Use this route for approved social media membership workflows. Common examples
include professional groups, customer groups, and topic-specific research
groups. Keep every membership decision tied to one account and Community ID.

## Understand Open, Restricted, and Invited Membership

X Communities offer different membership modes. An open Community displays a
Join button on its Communities page. A restricted Community can display an Ask
to join button instead. Admins and moderators review those requests.

Some restricted Communities allow member invitations. X says moderator
invitations receive automatic approval. Other restricted Communities accept
requests but disable member invitations.

The API request has no membership-mode field. It also has no invitation token.
It cannot bypass approval or Community rules. X evaluates the current account,
membership mode, invitation state, and moderation restrictions.

Use the official [X Communities guide](https://help.x.com/en/using-x/communities)
for current interface behavior. Use this page for Xquik's public API contract.
X can change its interface without changing this request body.

## Find Twitter Communities Before Joining

Choose the Community before calling this endpoint. X currently supports
Community discovery through its web and iOS interfaces. Search can use the
name, description, creator name, or creator handle.

X warns that not every Community appears in search results. A shared direct
URL can still open a Community page. Review that page before approving the
membership.

Do not use [Community Search](/api-reference/x/community-search) for discovery.
That read route searches Community posts after receiving a known numeric ID.
It does not find Twitter Communities by topic.

Evaluate each group against its target audience. Review the name, description,
rules, members, moderators, and recent Community posts. A dedicated space can
improve focus, but membership alone does not prove quality.

## Resolve Membership Intent

Start with a numeric Community ID. Display names can match across unrelated
groups. Request [Community Info](/api-reference/x/community-info) before the
write. Show its name, description, rules, and member count.

Show the connected account beside those details. Never confuse that profile
with a separate community account. Communities belong to X accounts and have
their own stable IDs.

Record the approval with these values:

- Community ID and verified display name.
- Connected username and stable account ID.
- Membership mode visible during review.
- Approval reference and approving operator.
- Idempotency key for this exact membership.
- Returned action ID and lifecycle state.

Do not reuse approval for another account. Each account and Community pair
represents a separate decision.

## How to Join Twitter Community Membership Safely

Create one idempotency key for the approved pair. Send the Community ID in the
path. Send the connected username through `account`.

Store the returned action immediately. A `202` response means processing has
started. It does not confirm membership. Poll the action until `terminal`
becomes `true`.

Use this sequence:

1. Resolve and review the numeric Community ID.
2. Confirm the connected account and approval.
3. Create one idempotency key for that pair.
4. Submit `POST /x/communities/{id}/join` once.
5. Poll the returned write action.
6. Verify the account through Community Members.
7. Store the result with the original approval.

Do not create new keys during a timeout. First inspect the existing action.
Changed accounts or Community IDs require new approvals and keys.

## Join Communities in Controlled Batches

Process each approved account and Community pair independently. Assign one
idempotency key to each pair. Keep retries serial for that pair.

Use a batch ledger when several accounts need membership. Each row should
contain one account, one Community ID, and one approval. Never overwrite earlier
attempts with the latest state.

Pause one account after an authorization error. Confirm its connection before
continuing. Other approved accounts can proceed through separate workers.

Do not treat membership as posting permission. Publishing Community posts is
another write decision. Require a separate approval and idempotency key.

## Verify Visible Membership

Wait for a terminal write result. Then call
[Community Members](/api-reference/x/community-members). Follow every required
cursor until the account appears or the list ends.

Store these verification facts:

1. Community ID and connected account.
2. Final action status and completion time.
3. Member lookup time and cursor coverage.
4. Matched username and stable user ID.
5. Operator decision when membership remains unconfirmed.

Do not resubmit after checking only the first page. Large member lists can span
many cursors. Finish the read before deciding.

X Communities' member lists are visible through direct Community URLs. X also
says anyone can view moderator and member lists. Do not present Community
membership as private.

## Explain Community Post Visibility

Community posts are not private group messages. X says posts can appear on the
Community page, profiles, timelines, and global search. Followers and other X
users may also see them.

Joining does not publish a post. It only changes membership for the selected
Twitter account. Use [Create Tweet](/api-reference/x-write/create-tweet) for a
separately approved Community post.

Membership can help a person join relevant conversations. Active participation
builds loyalty only when posts remain useful. It does not guarantee reach,
followers, replies, reposts, likes, or leads.

Brands may build a loyal member group through useful discussion. The join API
does not provide growth, moderation, engagement, or analytics services.

## Answer Common Twitter Community Questions

### How to Join a Twitter Community with Xquik?

Resolve its numeric ID and review Community Info. Submit the ID with one
connected account. Poll the returned action, then verify Community Members.

### Can I Join an Invitation-Only Twitter Community?

The request cannot create or accept an invitation token. X evaluates the
account's current invitation and eligibility state. Preserve any `422`
rejection for review.

### What Happens When You Join a Twitter Community?

The account becomes a member after X accepts the action. Its Communities tab
may then show the joined group. Membership can enable participation under the
Community's current rules.

### Can People See the Communities You Join on Twitter?

X says direct Community URLs expose member and moderator lists. Community posts
can also appear publicly. Treat membership and participation as discoverable.

### How Do I Join a Community on a Phone?

Use X's current iOS or Android interface for a manual join. This endpoint serves
software workflows. Its request stays identical across client platforms.

### Do Twitter Communities Still Exist?

Yes. X still publishes current Communities guidance. X controls availability,
search, membership modes, moderation, and interface placement.

### Do I Need X Premium to Join a Community?

This request has no Premium field. X decides current account eligibility.
Check the account and Community page before submitting a billable write.

### Why Can't I Join a Twitter Community?

Check the numeric ID, account connection, membership mode, and invitation
state. X currently requires a public account. A moderator block can also
prevent membership.

Review the returned status before another attempt. Invalid input, missing
credits, account authorization, rate limits, and X rejection require different
fixes.

### Is Community Notes the Same as X Communities?

No. Community Notes is a separate feature for adding context to posts. This
endpoint joins X Communities and does not enroll Community Notes contributors.

### Can This API Create, Moderate, or Grow a Community?

No. Use [Create Community](/api-reference/x-write/create-community) for a new
container. This route cannot enforce rules, appoint moderators, invite members,
manage engagement, or build an audience.

## Recover From Join Failures

Fix malformed IDs or bodies after `400`. Replace invalid credentials after
`401`. Restore credits after `402`. Reconnect the selected account after `403`.

Check both the account and Community after `404`. A missing account differs
from a missing Community. Preserve that distinction in operator messages.

For `409`, compare the saved account, Community ID, and request body. Reuse the
key only when every value still matches. Different inputs require new approval.

Preserve `422` rejection details. Do not substitute another Community
automatically. X may reject membership because of rules or account eligibility.

Honor retry guidance after `429`. Keep the approved account and Community
unchanged. After `500` or `503`, inspect `safeToRetry` and the action lifecycle.

Never infer failure from a dropped client connection. Verification prevents
duplicate requests and protects the membership ledger.

| Community join record | Request or response source | Membership rule |
| --- | --- | --- |
| Community ID | Path `{id}` | Confirm the reviewed group before joining. |
| Acting X account | Request `account` | Preserve the joining profile. |
| Community details | Community Info | Review name, rules, and member count. |
| Membership mode | X Community page | Never bypass approval or invitations. |
| Idempotency key | Request header | Assign one key per account and Community pair. |
| Write action ID | Response `id` | Poll the matching lifecycle record. |
| Lifecycle state | Response `status` and `terminal` | Stop only after a terminal state. |
| Membership proof | Community Members | Find the connected account after success. |
| Posting permission | Separate approval | Never infer permission from membership. |

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key. OAuth bearer authentication is also supported. Generate a key from the [dashboard](https://xquik.com/dashboard).
</ParamField>

<ParamField header="Idempotency-Key" type="string" required>
  Unique key for this intended write. Reuse it only for an exact network replay.
</ParamField>

<ParamField header="Content-Type" type="string" required>
  Must be `application/json`.
</ParamField>

## Path parameters

<ParamField path="id" type="string" required>
  The ID of the community to join.
</ParamField>

## Body

<ParamField body="account" type="string" required>
  The connected X account to join the community with. Must be a username you have connected to your Xquik account.
</ParamField>

## Response

<Tabs>
  <Tab title="404 Account Not Found">
    Connect the requested account, then submit a newly approved write.
  </Tab>
</Tabs>

<WriteActionLifecycleResponse />
