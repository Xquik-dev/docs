---
title: Create Monitor
description: Start monitoring an X account for events
---

## Request

```
POST /api/v1/monitors
```

### Headers

| Header | Required | Description |
|--------|----------|-------------|
| `x-api-key` | Yes | Your API key |
| `Content-Type` | Yes | `application/json` |

### Body

```json
{
  "username": "elonmusk",
  "eventTypes": ["tweet.new", "tweet.reply"]
}
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `username` | string | Yes | X username (1-15 chars, `@` prefix stripped) |
| `eventTypes` | string[] | Yes | At least 1 event type |

### Valid Event Types

`tweet.new`, `tweet.quote`, `tweet.reply`, `tweet.retweet`

## Response

<Tabs>
  <Tab title="201 Created">
    ```json
    {
      "id": "7",
      "username": "elonmusk",
      "xUserId": "44196397",
      "eventTypes": ["tweet.new", "tweet.reply"],
      "createdAt": "2026-02-24T10:30:00.000Z"
    }
    ```
  </Tab>
  <Tab title="403 Limit Reached">
    ```json
    {
      "error": "monitor_limit_reached",
      "limit": 1
    }
    ```
    Your plan allows 1 monitor by default. Add more for $10/month each from the dashboard.
  </Tab>
  <Tab title="409 Duplicate">
    ```json
    { "error": "monitor_already_exists" }
    ```
    You already have a monitor for this X account.
  </Tab>
</Tabs>
