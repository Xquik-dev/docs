---
title: "Analyze Tweet Performance & Writing Patterns"
sidebarTitle: "Analyze performance"
description: "Refresh likes, reposts, quotes, replies, bookmarks, and views for cached tweets. Compare current metrics with saved style records. See request fields."
api: "GET /styles/{id}/performance"
keywords: ["style", "performance", "engagement", "metrics", "likes", "retweets", "views"]
---

import LlmsDirective from "/snippets/llms-directive.mdx";

{/* GENERATED RESPONSE EXAMPLES START */}
<ResponseExample>
  ```json 200
  {
    "xUsername": "elonmusk",
    "tweetCount": 5,
    "tweets": [
      {
        "id": "1234567890",
        "text": "Excited to share our latest research findings.",
        "likeCount": 120,
        "retweetCount": 15,
        "replyCount": 8
      }
    ]
  }
  ```

  ```json 401
  {
    "error": "unauthenticated",
    "message": "Authentication required. Provide a valid API key or bearer token."
  }
  ```

  ```json 402
  {
    "error": "insufficient_credits",
    "message": "Insufficient credits. Top up or subscribe to continue."
  }
  ```

  ```json 404
  {
    "error": "not_found",
    "message": "Resource not found."
  }
  ```

  ```json 429
  {
    "error": "rate_limit_exceeded",
    "message": "Too many requests. Try again later.",
    "retryAfter": 60
  }
  ```
</ResponseExample>
{/* GENERATED RESPONSE EXAMPLES END */}
<LlmsDirective />

## Refresh Tweet Engagement for a Cached Style

Use this route after style analysis stores tweets for one profile. It refreshes
likes, reposts, quotes, replies, bookmarks, and views for those cached posts.
The result supports performance reviews without rebuilding the writing-style
profile.

Compare each returned tweet with its saved style record. Keep the tweet ID,
text, current engagement counts, and measurement time together. This makes
later comparisons reproducible when likes or replies continue changing.

The route charges per cached tweet. Check the cached tweet count before large
reviews. Use [Get Style](/api-reference/styles/get) for a free cache read when
fresh engagement metrics are unnecessary.

Use performance results to compare patterns, not to promise future reach. A
high-performing post can inform length, structure, or topic review. It does not
guarantee the next tweet will receive similar engagement.

<Callout icon="coins" color="#5c3327">
  **1 credit per tweet** · [All plans](https://xquik.com/#pricing) from $0.00012/credit
</Callout>

<CodeGroup>
  ```bash cURL
  curl -X GET https://xquik.com/api/v1/styles/elonmusk/performance \
    -H "x-api-key: xq_YOUR_KEY_HERE" | jq
  ```

  ```javascript Node.js
  const username = "elonmusk";
  const response = await fetch(`https://xquik.com/api/v1/styles/${username}/performance`, {
    method: "GET",
    headers: {
      "x-api-key": "xq_YOUR_KEY_HERE",
    },
  });
  const data = await response.json();
  ```

  ```python Python
  import requests

  username = "elonmusk"
  response = requests.get(
      f"https://xquik.com/api/v1/styles/{username}/performance",
      headers={"x-api-key": "xq_YOUR_KEY_HERE"},
  )
  data = response.json()
  ```

  ```go Go
  package main

  import (
      "encoding/json"
      "fmt"
      "net/http"
  )

  func main() {
      username := "elonmusk"
      req, err := http.NewRequest("GET", "https://xquik.com/api/v1/styles/"+username+"/performance", nil)
      if err != nil {
          panic(err)
      }
      req.Header.Set("x-api-key", "xq_YOUR_KEY_HERE")

      resp, err := http.DefaultClient.Do(req)
      if err != nil {
          panic(err)
      }
      defer resp.Body.Close()

      var data map[string]interface{}
      if err := json.NewDecoder(resp.Body).Decode(&data); err != nil {
          panic(err)
      }
      fmt.Println(data)
  }
  ```
</CodeGroup>

## Path parameters

<ParamField path="id" type="string" required>
  Style profile ID or X username whose cached tweets to analyze. Must have been cached with [Analyze & Cache Style](/api-reference/styles/analyze) first.
</ParamField>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key. Session cookie authentication is also supported. Generate a key from the [dashboard](https://xquik.com/dashboard).
</ParamField>

## Response

### 200 OK

<ResponseField name="xUsername" type="string">Normalized X username (lowercase, no `@` prefix).</ResponseField>
<ResponseField name="tweetCount" type="number">Number of tweets with engagement data.</ResponseField>
<ResponseField name="tweets" type="object[]">
  Array of tweets with engagement metrics.
  **Tweet object fields:**
    <ResponseField name="id" type="string">Tweet ID.</ResponseField>
    <ResponseField name="text" type="string">Tweet text content.</ResponseField>
    <ResponseField name="bookmarkCount" type="number">Number of bookmarks.</ResponseField>
    <ResponseField name="likeCount" type="number">Number of likes.</ResponseField>
    <ResponseField name="quoteCount" type="number">Number of quote tweets.</ResponseField>
    <ResponseField name="replyCount" type="number">Number of replies.</ResponseField>
    <ResponseField name="retweetCount" type="number">Number of retweets.</ResponseField>
    <ResponseField name="viewCount" type="number">Number of views.</ResponseField>

</ResponseField>

```json
{
  "xUsername": "elonmusk",
  "tweetCount": 20,
  "tweets": [
    {
      "id": "1893456789012345678",
      "text": "The future is now.",
      "bookmarkCount": 2340,
      "likeCount": 15420,
      "quoteCount": 1280,
      "replyCount": 890,
      "retweetCount": 3210,
      "viewCount": 1250000
    }
  ]
}
```

### 401 Unauthenticated

```json
{ "error": "unauthenticated", "message": "Missing or invalid API key" }
```
Missing or invalid API key.

### 402 Insufficient Credits

Possible error codes:

- `insufficient_credits` - Insufficient credits

```json
{ "error": "insufficient_credits", "message": "Insufficient credits. Top up or subscribe to continue." }
```
Top up or subscribe from the [dashboard billing page](https://dashboard.xquik.com/en/account?tab=subscription).

### 404 Not Found

```json
{ "error": "style_not_found", "message": "No cached style found for this username" }
```
No cached style exists for this username. Use [Analyze & Cache Style](/api-reference/styles/analyze) to fetch and cache tweets first.

### 429 Rate Limited

```json
{ "error": "rate_limit_exceeded", "message": "Too many requests. Try again later.", "retryAfter": 1 }
```
Too many requests. Wait for the `Retry-After` header before retrying.

<Info>
  Engagement metrics are fetched live from X at request time. Results may vary between calls as tweets gain interactions.
</Info>

<Note>
  **Related:** [Analyze & Cache Style](/api-reference/styles/analyze) to cache tweets before fetching metrics, [Get Style](/api-reference/styles/get) to view the cached tweet text, or [Compare Styles](/api-reference/styles/compare) to compare two accounts.
</Note>
